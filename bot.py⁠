import os
import requests

# Token va Chat ID endi kodda emas, GitHub Secrets orqali olinadi
BOT_TOKEN = os.environ.get("8997993468:AAGt8nAcu0TZCuLe8qBEiIgZpeTA9y2qXoE")
CHAT_ID = os.environ.get("-1003811990608")

def send_message():
    if not BOT_TOKEN or not CHAT_ID:
        print("XATO: BOT_TOKEN yoki CHAT_ID topilmadi")
        return

    url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
    payload = {
        "chat_id": CHAT_ID,
        "text": "Salom! Bu har kungi avtomatik xabar"
    }
    response = requests.post(url, json=payload)
    print(response.json())

if __name__ == "__main__":
    send_message()
